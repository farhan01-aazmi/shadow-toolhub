// Sidebar is now integrated directly into the homepage layout (Phase 14).
// This component is kept as an empty export to avoid import errors from other pages.
export default function Sidebar() {
    return null;
}
